[pytest]
markers =
    external: tests that require external network or third-party services
    auth: tests that require authentication flow (token issuance)
    unit: pure unit tests with isolated dependencies
